# Core LangGraph and LangChain dependencies
langgraph==0.2.28 
langchain-openai==0.2.2 
langchain==0.3.12

# HTTP requests and environment management
requests==2.32.3
python-dotenv==1.0.0

# Streamlit and data visualization
streamlit==1.32.0
pandas==2.2.0
plotly==5.18.0
numpy==1.24.3

# Standard library dependencies (included with Python)
# json, datetime, time, logging, os, typing - no need to install separately